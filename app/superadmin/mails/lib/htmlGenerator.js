import { SOCIAL_CONFIG } from '../_types';

const pad = (c) => ({
    pt: c.paddingTop ?? c.padding ?? 0,
    pr: c.paddingRight ?? c.padding ?? 0,
    pb: c.paddingBottom ?? c.padding ?? 0,
    pl: c.paddingLeft ?? c.padding ?? 0,
  });

export function generateHTML(blocks, settings) {
  const { bodyBg, containerBg, width, fontFamily, sectionGap } = settings;
  const safeWidth = Math.min(Math.max(parseInt(width) || 600, 300), 800);
  const safeFontFamily = fontFamily || 'Arial, sans-serif';
  const safeGap = Math.max(parseInt(sectionGap) || 0, 0);

  const getBgStyle = (content) => {
    let style = `background-color: ${content.bgColor || 'transparent'};`;
    if (content.bgImageUrl) { style += ` background-image: url('${content.bgImageUrl}');`; style += ` background-size: ${content.bgSize || 'cover'};`; style += ` background-position: ${content.bgPosition || 'center'};`; style += ` background-repeat: no-repeat;`; }
    return style;
  };

  const renderNestedBlock = (block, parentFont) => {
    const { type, content } = block;
    const bFont = (content.fontFamily && content.fontFamily !== 'inherit') ? content.fontFamily : (parentFont && parentFont !== 'inherit' ? parentFont : 'Arial, Helvetica, sans-serif');
    switch (type) {
      case 'text': { const a = content.align || 'left'; const s = Math.max(parseInt(content.fontSize) || 14, 12); const c = content.textColor || '#333333'; const w = content.fontWeight || 'normal'; return `<p align="${a}" style="color: ${c}; font-size: ${s}px; font-weight: ${w}; text-align: ${a}; margin: 0 0 10px 0; font-family: ${bFont}; line-height: 1.4;">${content.text ? content.text.replace(/\n/g, '<br/>') : ''}</p>`; }
      case 'image': { const w = Math.min(parseInt(content.width) || 100, 100); const r = parseInt(content.borderRadius) || 0; const a = content.align || 'center'; return `<table border="0" cellspacing="0" cellpadding="0" align="${a}" style="width: ${w}%;"><tr><td>${content.linkUrl ? `<a href="${content.linkUrl}" target="_blank" style="display: block; border: 0;">` : ''}<img src="${content.imageUrl}" alt="${content.altText || ''}" style="display: block; max-width: 100%; height: auto; border: 0; border-radius: ${r}px;" />${content.linkUrl ? '</a>' : ''}</td></tr></table>`; }
      case 'button': { const py = parseInt(content.paddingY) || 10; const px = parseInt(content.paddingX) || 20; const fs = Math.max(parseInt(content.fontSize) || 14, 12); const a = content.align || 'center'; const bf = (content.fontFamily && content.fontFamily !== 'inherit') ? content.fontFamily : 'Arial, sans-serif'; const bc = content.textColor || '#ffffff'; const bg = content.buttonBgColor || '#e11d48'; const bt = content.text || 'Botón'; const bw = content.fontWeight === 'bold' ? 'bold' : 'normal'; return `<table border="0" cellspacing="0" cellpadding="0" align="${a}" style="width: 100%;"><tr><td align="center" style="background-color: ${bg}; border-radius: ${parseInt(content.borderRadius) || 6}px; padding: ${py}px ${px}px;"><a href="${content.url || '#'}" target="_blank" style="display: inline-block; background-color: ${bg}; color: ${bc}; font-family: ${bf}; font-size: ${fs}px; font-weight: ${bw}; text-decoration: none;">${bt}</a></td></tr></table>`; }
      case 'video': return `<a href="${content.videoUrl || '#'}" target="_blank" style="display: block;"><img src="${content.coverUrl}" alt="Video" style="display: block; max-width: 100%; height: auto; border-radius: ${parseInt(content.borderRadius) || 0}px;" /></a>`;
      case 'social': return `<p style="font-size: 12px; color: #666;">[Bloque Social]</p>`;
      case 'divider': return `<div style="border-top: ${parseInt(content.height) || 1}px ${content.borderStyle || 'solid'} ${content.color || '#e5e7eb'}; margin: 10px 0;"></div>`;
      default: return '';
    }
  };

  const renderBlockHTML = (block) => {
    const { type, content } = block;
    const bFont = content.fontFamily && content.fontFamily !== 'inherit' ? content.fontFamily : safeFontFamily;
    const fontSize = Math.max(parseInt(content.fontSize) || 16, 12);
    const padding = parseInt(content.padding) || 20;
    const gap = safeGap;
    let blockHtml = '';
    switch (type) {
      case 'header': { const lw = Math.min(parseInt(content.logoWidth) || 600, safeWidth); const ha = content.align || 'center'; const hbs = getBgStyle(content); const hba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${hba} align="${ha}" style="${hbs} padding: ${padding}px;"><table border="0" cellspacing="0" cellpadding="0" align="${ha}"><tr><td><img src="${content.logoUrl}" alt="Logo" width="${lw}" style="display: block; max-width: ${lw}px; width: 100%; height: auto; border: 0;" /></td></tr></table></td></tr>`; break; }
      case 'text': { const tf = (content.fontFamily && content.fontFamily !== 'inherit') ? content.fontFamily : bFont; const tc = content.textColor || '#333333'; const ts = Math.max(parseInt(content.fontSize) || 16, 14); const tw = content.fontWeight || 'normal'; const ta = content.align || 'center'; const tlh = content.lineHeight || 1.5; const tbs = getBgStyle(content); const tba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; const pt = content.paddingTop ?? content.padding ?? 20; const pr = content.paddingRight ?? content.padding ?? 20; const pb = content.paddingBottom ?? content.padding ?? 20; const pl = content.paddingLeft ?? content.padding ?? 20; blockHtml = `<tr><td ${tba} align="${ta}" style="${tbs} padding-top: ${pt}px; padding-right: ${pr}px; padding-bottom: ${pb}px; padding-left: ${pl}px;"><p align="${ta}" style="color: ${tc}; font-size: ${ts}px; font-weight: ${tw}; text-align: ${ta}; margin: 0; font-family: ${tf}; line-height: ${tlh};">${content.text ? content.text.replace(/\n/g, '<br/>') : ''}</p></td></tr>`; break; }
      case 'image': { const iw = Math.floor((safeWidth * (parseInt(content.width) || 100)) / 100); const ir = parseInt(content.borderRadius) || 0; const ia = content.align || 'center'; const ibs = getBgStyle(content); const iba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${iba} align="${ia}" style="${ibs} padding: ${padding}px;"><table border="0" cellspacing="0" cellpadding="0" align="${ia}"><tr><td>${content.linkUrl ? `<a href="${content.linkUrl}" target="_blank" style="display: block; border: 0;">` : ''}<img src="${content.imageUrl}" alt="${content.altText || 'Imagen'}" width="${iw}" style="display: block; max-width: ${iw}px; width: 100%; height: auto; border: 0; border-radius: ${ir}px;" />${content.linkUrl ? '</a>' : ''}</td></tr></table></td></tr>`; break; }
      case 'video': { const vr = parseInt(content.borderRadius) || 0; const va = content.align || 'center'; const vbs = getBgStyle(content); const vba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; let vt = content.coverUrl || ''; const vu = content.videoUrl || ''; const ym = vu.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/|youtube\.com\/embed\/)([a-zA-Z0-9_-]+)/); if (ym && ym[1]) vt = vt || `https://img.youtube.com/vi/${ym[1]}/maxresdefault.jpg`; const vm = vu.match(/vimeo\.com\/(\d+)/); if (vm && vm[1] && !vt) vt = `https://vumbnail.com/${vm[1]}.jpg`; if (!vt) vt = 'https://placehold.co/600x338/181818/ffffff?text=▶+Click+para+ver'; blockHtml = `<tr><td ${vba} align="${va}" style="${vbs} padding: ${padding}px;"><table border="0" cellspacing="0" cellpadding="0" align="${va}"><tr><td><a href="${vu || '#'}" target="_blank" style="display: block; border: 0; text-decoration: none;"><img src="${vt}" alt="Video" style="display: block; max-width: 100%; border-radius: ${vr}px; border: 0;" /></a></td></tr></table></td></tr>`; break; }
      case 'columns': { const cc = Math.max(Math.min(parseInt(content.colCount) || 2, 4), 1); const cw = Math.floor(100 / cc); const cp = parseInt(content.padding) || 20; const cbs = getBgStyle(content); const cba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; let ci = ''; for (let i = 0; i < cc; i++) { let colC = ''; if (content.cols && content.cols[i]) colC = content.cols[i].map(b => `<tr><td style="padding: 10px 0;">${renderNestedBlock(b, bFont)}</td></tr>`).join(''); ci += `<td width="${cw}%" style="width: ${cw}%; vertical-align: ${content.align === 'middle' ? 'middle' : content.align === 'bottom' ? 'bottom' : 'top'}; padding: ${cp}px;"><table width="100%" border="0" cellspacing="0" cellpadding="0">${colC}</table></td>`; } blockHtml = `<tr><td ${cba} style="${cbs} padding: 0;"><table width="100%" border="0" cellspacing="0" cellpadding="0" style="width: 100% !important;"><tr>${ci}</tr></table></td></tr>`; break; }
      case 'button': { const br = parseInt(content.borderRadius) || 6; const bpy = parseInt(content.paddingY) || 15; const bpx = parseInt(content.paddingX) || 30; const bfs = Math.max(parseInt(content.fontSize) || 16, 14); const ba = content.align || 'center'; const bff = (content.fontFamily && content.fontFamily !== 'inherit') ? content.fontFamily : 'Arial, sans-serif'; const btc = content.textColor || '#ffffff'; const bbg = content.buttonBgColor || '#e11d48'; const bbt = content.text || 'Botón'; const bu = content.url || '#'; const bw = content.fontWeight === 'bold' ? 'bold' : 'normal'; const bp = parseInt(content.padding) || 10; const cbs = getBgStyle({ bgColor: content.containerBgColor, bgImageUrl: content.bgImageUrl, bgSize: content.bgSize, bgPosition: content.bgPosition }); const cba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${cba} align="${ba}" style="${cbs} padding: ${bp}px;"><table border="0" cellspacing="0" cellpadding="0" align="${ba}"><tr><td style="background-color: ${bbg}; border-radius: ${br}px; -webkit-border-radius: ${br}px; -moz-border-radius: ${br}px;"><table border="0" cellspacing="0" cellpadding="0"><tr><td style="padding: ${bpy}px ${bpx}px; background-color: ${bbg}; border-radius: ${br}px; -webkit-border-radius: ${br}px; -moz-border-radius: ${br}px;"><a href="${bu}" target="_blank" style="display: inline-block; background-color: ${bbg}; color: ${btc}; font-family: ${bff}; font-size: ${bfs}px; font-weight: ${bw}; text-decoration: none; border-radius: ${br}px;"><!--[if mso]><span style="color: ${btc}; font-family: Arial, sans-serif; font-size: ${bfs}px; font-weight: ${bw};">${bbt}</span><![endif]--><![if !mso]><span style="color: ${btc}; font-family: ${bff}; font-size: ${bfs}px; font-weight: ${bw};">${bbt}</span><![endif]></a></td></tr></table></td></tr></table></td></tr>`; break; }
      case 'divider': { const dh = Math.max(parseInt(content.height) || 1, 1); const dbs = getBgStyle(content); const dba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${dba} style="${dbs} padding: ${padding}px;"><table width="100%" border="0" cellspacing="0" cellpadding="0"><tr><td style="border-top: ${dh}px ${content.borderStyle || 'solid'} ${content.color || '#e5e7eb'}; font-size: 1px; line-height: 1px; height: ${dh}px;">&nbsp;</td></tr></table></td></tr>`; break; }
      case 'spacer': { const sh = Math.max(parseInt(content.height) || 30, 10); const sbs = getBgStyle(content); const sba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${sba} style="${sbs} height: ${sh}px; font-size: 1px; line-height: ${sh}px;">&nbsp;</td></tr>`; break; }
      case 'social': { const is = Math.max(parseInt(content.iconSize) || 24, 16); const sr = parseInt(content.borderRadius) || 8; const sa = content.align || 'center'; const sbs = getBgStyle(content); const sba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; let si = ''; if (content.networks && content.networks.length > 0) { content.networks.forEach(n => { const cfg = SOCIAL_CONFIG[n.network]; if (cfg) { const ch = (n.iconColor || '#ffffff').replace('#', ''); si += `<td style="padding: 5px;"><a href="${n.url}" target="_blank"><table border="0" cellspacing="0" cellpadding="0"><tr><td width="${is + 16}" height="${is + 16}" style="background-color: ${n.bgColor || cfg.defaultBg}; border-radius: ${sr}px; text-align: center; vertical-align: middle;"><img src="https://img.icons8.com/ios-filled/50/${ch}/${cfg.iconName}.png" width="${is}" height="${is}" style="display: block; margin: 8px auto;" alt="${cfg.label}" /></td></tr></table></a></td>`; } }); } blockHtml = `<tr><td ${sba} align="${sa}" style="${sbs} padding: ${padding}px;"><table border="0" cellspacing="0" cellpadding="0" align="${sa}"><tr>${si}</tr></table></td></tr>`; break; }
      case 'html': { let hc = content.code || ''; if (hc.includes('<!DOCTYPE') || hc.includes('<html')) { const bm = hc.match(/<body[^>]*>([\s\S]*?)<\/body>/i); hc = bm ? bm[1].trim() : hc; } const mm = hc.match(/google\.com\/maps\/embed\?pb=([^"'\s]+)/); const hbs = getBgStyle(content); const hba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; if (mm || hc.includes('google.com/maps')) { blockHtml = `<tr><td ${hba} align="center" style="${hbs} padding: ${padding}px;"><a href="https://www.google.com/maps" target="_blank" style="display: block; border: 0; text-decoration: none;"><table border="0" cellspacing="0" cellpadding="0"><tr><td style="background-color: #E8F4E8; padding: 30px; border-radius: ${parseInt(content.borderRadius) || 8}px; text-align: center;"><img src="https://maps.gstatic.com/tactile/icons/geo_pinlet-2x.png" alt="Mapa" style="display: block; margin: 0 auto 15px auto;" /><p style="color: #1a7f1a; font-size: 16px; font-family: Arial, sans-serif; margin: 0; font-weight: bold;">📍 Ver ubicación en Google Maps</p></td></tr></table></a></td></tr>`; } else { blockHtml = `<tr><td ${hba} style="${hbs} padding: 0;">${hc}</td></tr>`; } break; }
      case 'receipt': { const rc = content; const bg = rc.bgColor || '#111111'; const ac = rc.accentColor || '#4ade80'; const hBg = rc.headerBg || '#1a1a1a'; const r1 = rc.rowBg1 || '#141414'; const r2 = rc.rowBg2 || '#111111'; const tBg = rc.totalBg || '#0a1a0f'; const items = rc.items || []; const rh = items.map((item, i) => { const bg_ = i % 2 === 0 ? r1 : r2; const ih = item.imagen ? `<img src="${item.imagen}" alt="${item.nombre}" width="40" height="40" style="display:inline-block;vertical-align:middle;border-radius:8px;object-fit:cover;border:1px solid #333333;margin-right:10px;" />` : ''; return `<tr style="background-color:${bg_};"><td style="padding:12px 16px;border-top:1px solid #222222;font-family:Arial,sans-serif;"><table border="0" cellspacing="0" cellpadding="0"><tr>${item.imagen ? `<td style="padding-right:10px;vertical-align:middle;">${ih}</td>` : ''}<td style="vertical-align:middle;"><p style="margin:0;font-size:13px;font-weight:700;color:#ffffff;font-family:Arial,sans-serif;">${item.nombre}</p><p style="margin:3px 0 0;font-size:11px;color:#6b7280;font-family:Arial,sans-serif;">${item.categoria}</p></td></tr></table></td><td style="padding:12px 16px;text-align:center;font-size:13px;color:#9ca3af;border-top:1px solid #222222;font-family:Arial,sans-serif;">1</td><td style="padding:12px 16px;text-align:right;font-size:14px;font-weight:900;color:${ac};border-top:1px solid #222222;font-family:Arial,sans-serif;">$${item.precio}</td></tr>`; }).join(''); const dr = rc.showDiscount ? `<tr style="background-color:${hBg};"><td colspan="2" style="padding:6px 16px 12px;font-size:12px;color:#f59e0b;text-align:right;font-family:Arial,sans-serif;">Descuento (${rc.cuponVar || '{{cupon}}'})</td><td style="padding:6px 16px 12px;text-align:right;font-size:13px;font-weight:700;color:#f59e0b;font-family:Arial,sans-serif;">-$${rc.descuentoVar || '{{descuento_monto}}'}</td></tr>` : ''; blockHtml = `<tr><td style="background-color:${bg};padding:${rc.padding || '24px'};"><table width="100%" border="0" cellspacing="0" cellpadding="0" style="font-family:Arial,sans-serif;"><tr style="background-color:${hBg};"><td style="padding:10px 14px;font-size:10px;font-weight:900;color:#6b7280;letter-spacing:2px;text-transform:uppercase;width:55%;">Producto</td><td style="padding:10px 14px;font-size:10px;font-weight:900;color:#6b7280;letter-spacing:2px;text-transform:uppercase;text-align:center;width:15%;">Cant.</td><td style="padding:10px 14px;font-size:10px;font-weight:900;color:#6b7280;letter-spacing:2px;text-transform:uppercase;text-align:right;width:30%;">Precio</td></tr>${rh}<tr style="background-color:${hBg};"><td colspan="2" style="padding:12px 16px;font-size:12px;color:#6b7280;text-align:right;border-top:1px solid #222222;font-family:Arial,sans-serif;">Subtotal</td><td style="padding:12px 16px;text-align:right;font-size:13px;font-weight:700;color:#9ca3af;border-top:1px solid #222222;font-family:Arial,sans-serif;">$${rc.subtotalVar || '{{subtotal}}'}</td></tr>${dr}<tr style="background-color:${tBg};"><td colspan="2" style="padding:16px;font-size:11px;font-weight:900;color:${ac};letter-spacing:2px;text-transform:uppercase;text-align:right;font-family:Arial,sans-serif;">TOTAL PAGADO</td><td style="padding:16px;text-align:right;font-size:22px;font-weight:900;color:${ac};font-family:Arial,sans-serif;">$${rc.totalVar || '{{total}}'}</td></tr></table><table width="100%" border="0" cellspacing="0" cellpadding="0" style="margin-top:10px;font-family:Arial,sans-serif;"><tr><td width="49%" style="padding:14px 16px;background-color:#161616;border-radius:10px;"><p style="margin:0;font-size:10px;color:#6b7280;text-transform:uppercase;letter-spacing:2px;font-weight:900;font-family:Arial,sans-serif;">Método de Pago</p><p style="margin:6px 0 0;font-size:13px;font-weight:700;color:#ffffff;font-family:Arial,sans-serif;">${rc.metodoPagoVar || '{{metodo_pago}}'}</p></td><td width="2%"></td><td width="49%" style="padding:14px 16px;background-color:#161616;border-radius:10px;"><p style="margin:0;font-size:10px;color:#6b7280;text-transform:uppercase;letter-spacing:2px;font-weight:900;font-family:Arial,sans-serif;">Fecha</p><p style="margin:6px 0 0;font-size:13px;font-weight:700;color:#ffffff;font-family:Arial,sans-serif;">${rc.fechaVar || '{{fecha}}'}</p></td></tr></table></td></tr>`; break; }
      case 'footer': { const ffs = Math.max(parseInt(content.fontSize) || 12, 10); const ff = content.fontFamily && content.fontFamily !== 'inherit' ? content.fontFamily : bFont; const fa = content.align || 'center'; const fbs = getBgStyle(content); const fba = content.bgImageUrl ? ` background="${content.bgImageUrl}"` : ''; blockHtml = `<tr><td ${fba} align="${fa}" style="${fbs} padding: ${padding}px;"><p align="${fa}" style="color: ${content.textColor || '#6b7280'}; font-size: ${ffs}px; font-family: ${ff}, Arial, sans-serif; margin: 0; line-height: 1.5; text-align: ${fa};">${content.text ? content.text.replace(/\n/g, '<br/>') : ''}</p></td></tr>`; break; }
      default: blockHtml = '';
    }
    return blockHtml;
  };

  const blocksHtml = blocks.map(b => renderBlockHTML(b)).join('\n');
  return `<!DOCTYPE html>
<html lang="es" xmlns="http://www.w3.org/1999/xhtml" xmlns:v="urn:schemas-microsoft-com:vml" xmlns:o="urn:schemas-microsoft-com:office:office">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="x-apple-disable-message-reformatting">
  <title></title>
  <style>
    body, table, td, p, a, li { -webkit-text-size-adjust: 100%; -ms-text-size-adjust: 100%; }
    table, td { mso-table-lspace: 0pt; mso-table-rspace: 0pt; }
    img { -ms-interpolation-mode: bicubic; border: 0; outline: none; text-decoration: none; }
    body { height: 100% !important; margin: 0 !important; padding: 0 !important; width: 100% !important; }
    a[x-apple-data-detectors] { color: inherit !important; text-decoration: none !important; font-size: inherit !important; font-family: inherit !important; font-weight: inherit !important; line-height: inherit !important; }
    @media only screen and (max-width: 480px) { .mobile-padding { padding: 20px 10px !important; } .mobile-full { width: 100% !important; display: block !important; } }
  </style>
</head>
<body style="margin: 0; padding: 0; background-color: ${bodyBg}; font-family: ${safeFontFamily};">
  <table width="100%" border="0" cellspacing="0" cellpadding="0" style="background-color: ${bodyBg};">
    <tr>
      <td align="center" style="padding: 20px 10px;">
        <table width="${safeWidth}" border="0" cellspacing="0" cellpadding="0" style="max-width: ${safeWidth}px; width: 100%; background-color: ${containerBg}; border-radius: 8px;">
          ${blocksHtml}
        </table>
      </td>
    </tr>
  </table>
</body>
</html>`;
}